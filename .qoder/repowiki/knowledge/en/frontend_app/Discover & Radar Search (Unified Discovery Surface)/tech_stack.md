React + `react-router-dom` for URL-driven mode selection; `@tanstack/react-query` with `useInfiniteQuery` for paginated results; `motion/react` (`AnimatePresence`) for tab transitions; `react-intersection-observer` for scroll-triggered pagination; `tesseract.js` loaded dynamically (`await import('tesseract.js')`) for client-side OCR; external `trace.moe` API for anime frame matching.